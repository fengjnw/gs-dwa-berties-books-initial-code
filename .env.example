# Database Configuration
# Update these values with your database credentials
DB_HOST=localhost
DB_USER=app_user
DB_PASSWORD=app_2027
DB_NAME=app_database

# Session Secret
# Change this secret key in production for security
SESSION_SECRET=your-secret-key-change-this-in-production

# Base Path Configuration
# For local development, leave empty or set to empty string
# For production deployment in subdirectory, set to the subdirectory path
# Example for cloud deployment at /usr/347/:
# BASE_PATH=/usr/347
BASE_PATH=
