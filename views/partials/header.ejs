<!doctype html>
<html>

<head>
    <title>
        <%= typeof pageTitle !=='undefined' ? pageTitle : shopData.shopName %>
    </title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
</head>

<body>
    <h1>
        <%= shopData.shopName %>
    </h1>
    <p>
        <% if (typeof session !=='undefined' && session.userId) { %>
            Logged in as: <strong>
                <%= session.userId %>
            </strong> | <a href="<%= basePath %>/users/logout">Logout</a>
            <% } else { %>
                Not logged in | <a href="<%= basePath %>/users/login">Login</a> | <a
                    href="<%= basePath %>/users/register">Register</a>
                <% } %>
    </p>
    <hr>